#!/bin/bash

RELROOT="./`git rev-parse --show-cdup`"
ROOTDIR="$(realpath $RELROOT)"

echo " ==> Using root : $ROOTDIR <=="

cd $ROOTDIR

git submodule init
git submodule update

source $ROOTDIR/poky/oe-init-build-env $ROOTDIR/build
